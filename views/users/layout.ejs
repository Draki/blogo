<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="utf-8">
		<link rel="stylesheet" type="text/css"	href="/stylesheets/style.css">
		<link rel="stylesheet" type="text/css"	href="/stylesheets/bootstrap.min.css">
		<script src="http://code.jquery.com/jquery.min.js"></script>
		<script src="/js/bootstrap.min.js"></script>
		<title>Blog SWCM</title>
	</head>
	<body>
		<header>
			<form method="GET" action="/posts/search">
				<input type="text"	name="buscar" size="14">
				<input type="submit" value="Buscar">
			</form>

<br /><br /><br /><br />
			<section>
				<% var flash = flash() %>
				<% if (flash.info) { %>
				<article id="flashinfo">
					<h3>Información de interés:</h3>
					<ul>
						<% for (var i in flash.info) { %>
						<li>
							<%= flash.info[i] %>
						</li>
						<% } %>
					</ul>
				</article>
				<% } %>
				<% if (flash.success) { %>
				<article id="flashsuccess">
					<h3>Tareas completadas con éxito:</h3>
					<ul>
						<% for (var i in flash.success) { %>
						<li>
							<%= flash.success[i] %>
						</li>
						<% } %>
					</ul>
				</article>
				<% } %>
				<% if (flash.error) { %>
				<article id="flasherror">
					<h3>Errores encontrados:</h3>
					<ul>
						<% for (var i in flash.error) { %>
						<li>
							<%= flash.error[i] %>
						</li>
						<% } %>
					</ul>
				</article>
				<% } %>
			</section>
			<div class="navbar navbar-inverse navbar-fixed-top">
				<div class="navbar-inner">
					<div class="container">
						<button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
							<span class="icon-bar"></span>
							<span class="icon-bar"></span>
							<span class="icon-bar"></span>
						</button>
						<a class="brand" href="/">VideoGame Maniac</a>
						<div class="nav-collapse collapse">
							<ul class="nav">
								<a href="/">Home</a>
								<a href="/posts">Posts</a>
								<a href="/users">Usuarios</a>
								<% if (session.user) { %>
								<a href="/users/<%= session.user.id %>/favourites"> Favoritos </a>
								<% } %>
							</ul>
							<form class="navbar-form pull-right" action="/posts/search" method="post">
								<input type="text" class="search-query" placeholder="Search" name="query" />
							</form>

							<form class="navbar-form pull-right" id='logininfo'>
								<% if (session.user) { %>
								<a href='/logout' class="btn btn-danger">Logout</a>
								<% } else { %>
								<a href='/login?redir=/users' class="btn btn-success">Login</a>
								<a href='/users/new' class="btn">Register</a>
								<% } %>
							</form>
						</div><!--/.nav-collapse -->
					</div>

					<div class="pull-right">
						<% if (locals.paginate_view) { %>
						<%- paginate_view %>
						<% } %>
					</div>
				</div>

		</header>
		<hr />
		<div>
			<%- body %>
		</div>
		<hr />

		<footer>
			<a href="/">Home</a>
			Curso 2012-2013
			<br />
			contador de visitas: <%= visitas %>
		</footer>
	</body>
</html>
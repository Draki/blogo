<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="utf-8">
		<link href="/stylesheets/style.css" type="text/css" rel="stylesheet">
		<link href="/stylesheets/bootstrap.min.css" type="text/css" rel="stylesheet">
		<style>
			body {
				padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
			}
		</style>
		<link href="/stylesheets/bootstrap-responsive.min.css" type="text/css" rel="stylesheet">

		<script src="http://code.jquery.com/jquery.min.js"></script>
		<script src="/js/bootstrap.min.js"></script>
		<title>VideoGame Maniac</title>
	</head>
	<body>
		<header>
			<div class="navbar navbar-inverse navbar-fixed-top">
				<div class="navbar-inner">
					<div class="container">
						<a class="brand" href="/">VideoGame Maniac</a>
						<div class="nav-collapse collapse">
							<ul class="nav">
								<li>
									<a href="/posts">Posts</a>
								</li>
								<li>
									<a href="/users">Usuarios</a>
								</li>

								<% if (session.user) { %>
								<li>
									<a href="/users/<%= session.user.id %>/favourites"> Favoritos </a>
								</li>
								<% } %>
							</ul>
							<form class="navbar-form pull-right" id='logininfo'>
								<% if (session.user) { %>
								<a href='/logout' class="btn btn-danger">Logout</a>
								<% } else { %>
								<a href='/login?redir=/' class="btn">Login</a>
								<a href='/users/new' class="btn">Nuevo usuario</a>
								<% } %>
							</form>
							<form class="navbar-form pull-right" action="/posts/search" method="post">
								<input type="text" class="search-query" placeholder="Search" name="query" />
							</form>
						</div>
					</div>

					<div class="pull-right">
						<% if (locals.paginate_view) { %>
						<%- paginate_view %>
						<% } %>
					</div>
				</div>
			</div>

			<div class="cuerpo">
				<h1>Videogame Maniac</h1>
				<h2>&nbsp;Reviews de videojuegos</h2>
				<h3>&nbsp;Por: Daniel Rodríguez Rodríguez y Álvaro García Albaladejo</h3>
				<section>
					<% var flash = flash() %>
					<% if (flash.info) { %>
					<article id="flashinfo">
						<h3>Información de interés:</h3>
						<ul>
							<% for (var i in flash.info) { %>
							<li>
								<%= flash.info[i] %>
							</li>
							<% } %>
						</ul>
					</article>
					<% } %>
					<% if (flash.success) { %>
					<article id="flashsuccess">
						<h3>Tareas completadas con éxito:</h3>
						<ul>
							<% for (var i in flash.success) { %>
							<li>
								<%= flash.success[i] %>
							</li>
							<% } %>
						</ul>
					</article>
					<% } %>
					<% if (flash.error) { %>
					<article id="flasherror">
						<h3>Errores encontrados:</h3>
						<ul>
							<% for (var i in flash.error) { %>
							<li>
								<%= flash.error[i] %>
							</li>
							<% } %>
						</ul>
					</article>
					<% } %>
				</section>

		</header>
		<hr />
		<div>
			<%- body %>
		</div>
		<hr />

		<footer>
			contador de visitas: <%= visitas %>
		</footer>

		</div>
	</body>
</html>